{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna exec -- rimraf dist/",
    "build": "lerna run build",
    "test": "jest"
  }
}
